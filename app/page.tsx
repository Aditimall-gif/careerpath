'use client'

import { useMemo, useState } from 'react'
import { Button } from '@/components/ui/button'
import {
  Bell, Bookmark, BriefcaseBusiness, Building2, Check, ChevronDown, CircleUserRound,
  Clock3, FileText, Heart, LayoutDashboard, MapPin, Menu, Plus, Search, Settings2,
  SlidersHorizontal, Sparkles, Users, X, Zap,
} from 'lucide-react'

const seedJobs = [
  { id: 1, company: 'Linear', mark: 'L', title: 'Product Designer', location: 'Remote · US', type: 'Full-time', salary: '$145k–$175k', posted: '2h ago', tags: ['Product', 'Figma', 'Systems'], color: 'bg-foreground', featured: true },
  { id: 2, company: 'Arcade', mark: 'A', title: 'Senior Product Manager', location: 'New York, NY', type: 'Full-time', salary: '$160k–$195k', posted: '5h ago', tags: ['B2B', 'Strategy', 'Growth'], color: 'bg-chart-3', featured: false },
  { id: 3, company: 'Vercel', mark: 'V', title: 'Design Engineer', location: 'Remote · Worldwide', type: 'Full-time', salary: '$170k–$210k', posted: '1d ago', tags: ['React', 'Design', 'Frontend'], color: 'bg-primary', featured: true },
  { id: 4, company: 'Notion', mark: 'N', title: 'Brand Designer', location: 'San Francisco, CA', type: 'Full-time', salary: '$130k–$165k', posted: '1d ago', tags: ['Brand', 'Motion', 'Creative'], color: 'bg-chart-4', featured: false },
  { id: 5, company: 'Ramp', mark: 'R', title: 'UX Researcher', location: 'New York, NY', type: 'Full-time', salary: '$135k–$170k', posted: '2d ago', tags: ['Research', 'Fintech', 'UX'], color: 'bg-chart-2', featured: false },
]
const seedApplications = [
  { company: 'Figma', role: 'Product Designer', status: 'Interview', date: 'Applied Aug 28', mark: 'F' },
  { company: 'Stripe', role: 'Senior Designer', status: 'Applied', date: 'Applied Aug 25', mark: 'S' },
  { company: 'Airbnb', role: 'Design Lead', status: 'Offer', date: 'Applied Aug 18', mark: 'A' },
]

type Job = typeof seedJobs[number]

function Logo({ mark, color = 'bg-foreground' }: { mark: string; color?: string }) {
  return <div className={`flex size-10 shrink-0 items-center justify-center rounded-xl text-sm font-bold text-primary-foreground ${color}`}>{mark}</div>
}
function NavItem({ icon: Icon, label, active, onClick, count }: { icon: any; label: string; active?: boolean; onClick: () => void; count?: number }) {
  return <button onClick={onClick} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm font-medium transition ${active ? 'bg-accent text-accent-foreground' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon className="size-4" />{label}{count ? <span className="ml-auto rounded-full bg-muted px-2 py-0.5 text-xs">{count}</span> : null}</button>
}
function JobCard({ job, saved, onSave, onOpen, onApply }: { job: Job; saved: boolean; onSave: () => void; onOpen: () => void; onApply: () => void }) {
  return <article className="group rounded-2xl border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><Logo mark={job.mark} color={job.color} /><div><h3 className="font-semibold leading-tight">{job.title}</h3><p className="mt-1 text-sm text-muted-foreground">{job.company}</p></div></div><button aria-label={saved ? 'Remove saved job' : 'Save job'} onClick={onSave} className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground">{saved ? <Bookmark className="size-4 fill-current" /> : <Bookmark className="size-4" />}</button></div><div className="mt-5 flex flex-wrap gap-x-4 gap-y-2 text-xs text-muted-foreground"><span className="flex items-center gap-1"><MapPin className="size-3.5" />{job.location}</span><span className="flex items-center gap-1"><BriefcaseBusiness className="size-3.5" />{job.type}</span><span>{job.salary}</span></div><div className="mt-4 flex flex-wrap gap-1.5">{job.tags.map(tag => <span key={tag} className="rounded-md bg-muted px-2 py-1 text-[11px] text-muted-foreground">{tag}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted-foreground">{job.posted}</span><div className="flex gap-2"><Button variant="ghost" size="sm" onClick={onOpen}>View</Button><Button size="sm" onClick={onApply}><Zap data-icon="inline-start" />Quick apply</Button></div></div></article>
}

export default function Page() {
  const [section, setSection] = useState('Overview')
  const [employer, setEmployer] = useState(false)
  const [query, setQuery] = useState('')
  const [saved, setSaved] = useState<number[]>([3])
  const [applications, setApplications] = useState(seedApplications)
  const [selected, setSelected] = useState<Job | null>(null)
  const [toast, setToast] = useState('')
  const [showPost, setShowPost] = useState(false)
  const [mobileNav, setMobileNav] = useState(false)

  const filtered = useMemo(() => seedJobs.filter(job => `${job.title} ${job.company} ${job.location} ${job.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())), [query])
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  const apply = (job: Job) => { if (!applications.some(a => a.company === job.company && a.role === job.title)) setApplications(prev => [{ company: job.company, role: job.title, status: 'Applied', date: 'Applied just now', mark: job.mark }, ...prev]); setSelected(null); setSection('Applications'); notify(`Application sent to ${job.company}`) }
  const nav = (item: string) => { setSection(item); setMobileNav(false) }

  return <div className="min-h-screen bg-background text-foreground"><aside className={`fixed inset-y-0 left-0 z-20 flex w-64 flex-col border-r border-border bg-card px-4 py-5 transition-transform lg:translate-x-0 ${mobileNav ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center gap-2 px-2"><div className="flex size-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Sparkles className="size-4" /></div><span className="font-semibold tracking-tight">Northstar</span></div><div className="mt-10 flex flex-1 flex-col gap-1"><p className="px-3 pb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Workspace</p><NavItem icon={LayoutDashboard} label="Overview" active={section === 'Overview'} onClick={() => nav('Overview')} /><NavItem icon={Search} label="Find jobs" active={section === 'Find jobs'} onClick={() => nav('Find jobs')} /><NavItem icon={FileText} label="Applications" active={section === 'Applications'} count={applications.length} onClick={() => nav('Applications')} /><NavItem icon={Bookmark} label="Saved" active={section === 'Saved'} count={saved.length} onClick={() => nav('Saved')} /><NavItem icon={CircleUserRound} label="Profile" active={section === 'Profile'} onClick={() => nav('Profile')} /><div className="mt-auto"><NavItem icon={Settings2} label="Settings" onClick={() => notify('Settings are coming soon')} /></div></div><div className="rounded-2xl bg-muted p-3"><div className="flex items-center gap-2"><div className="flex size-8 items-center justify-center rounded-full bg-accent text-xs font-bold">JD</div><div className="min-w-0"><p className="truncate text-sm font-semibold">Jordan Davis</p><p className="truncate text-xs text-muted-foreground">Product designer</p></div><ChevronDown className="ml-auto size-4 text-muted-foreground" /></div></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-border bg-background/90 px-5 backdrop-blur lg:px-8"><button className="rounded-lg p-2 hover:bg-muted lg:hidden" onClick={() => setMobileNav(true)} aria-label="Open navigation"><Menu className="size-5" /></button><div className="hidden text-sm text-muted-foreground lg:block">{employer ? 'Employer workspace' : `Good morning, Jordan`}</div><div className="flex items-center gap-3"><button onClick={() => notify('You are all caught up')} className="rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Notifications"><Bell className="size-4" /></button><div className="flex items-center gap-2 border-l border-border pl-3"><span className="hidden text-sm font-medium sm:block">{employer ? 'Hiring mode' : 'Job seeker'}</span><button onClick={() => setEmployer(!employer)} className={`relative h-6 w-11 rounded-full transition ${employer ? 'bg-primary' : 'bg-muted'}`} aria-label="Toggle employer mode"><span className={`absolute top-1 size-4 rounded-full bg-background transition ${employer ? 'left-6' : 'left-1'}`} /></button></div></div></header><main className="mx-auto max-w-7xl p-5 lg:p-8">{employer ? <Employer onPost={() => setShowPost(true)} notify={notify} /> : <>{section === 'Overview' && <Overview nav={nav} jobs={filtered} saved={saved} setSaved={setSaved} setSelected={setSelected} apply={apply} />}{section === 'Find jobs' && <FindJobs query={query} setQuery={setQuery} jobs={filtered} saved={saved} setSaved={setSaved} setSelected={setSelected} apply={apply} />}{section === 'Saved' && <FindJobs query={query} setQuery={setQuery} jobs={seedJobs.filter(j => saved.includes(j.id))} saved={saved} setSaved={setSaved} setSelected={setSelected} apply={apply} title="Saved jobs" />}{section === 'Applications' && <Applications applications={applications} />}{section === 'Profile' && <Profile notify={notify} />}</>}</main></div>{selected && <Modal onClose={() => setSelected(null)}><div className="flex items-start gap-3"><Logo mark={selected.mark} color={selected.color} /><div><p className="text-sm text-muted-foreground">{selected.company}</p><h2 className="mt-1 text-2xl font-semibold tracking-tight">{selected.title}</h2></div></div><div className="mt-6 flex flex-wrap gap-2 text-sm text-muted-foreground"><span className="rounded-lg bg-muted px-3 py-2">{selected.location}</span><span className="rounded-lg bg-muted px-3 py-2">{selected.salary}</span></div><p className="mt-6 text-sm leading-6 text-muted-foreground">Join a thoughtful team building products that help people do their best work. This role blends craft, strategy, and close collaboration across the company.</p><div className="mt-8 flex justify-end gap-2"><Button variant="outline" onClick={() => setSelected(null)}>Close</Button><Button onClick={() => apply(selected)}><Zap data-icon="inline-start" />Quick apply</Button></div></Modal>}{showPost && <Modal onClose={() => setShowPost(false)}><h2 className="text-xl font-semibold">Post a new role</h2><div className="mt-5 flex flex-col gap-4"><input className="h-11 rounded-xl border border-input bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring" placeholder="Job title" /><input className="h-11 rounded-xl border border-input bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring" placeholder="Location or remote" /><textarea className="min-h-28 rounded-xl border border-input bg-background p-3 text-sm outline-none focus:ring-2 focus:ring-ring" placeholder="Tell candidates about the role" /><Button onClick={() => { setShowPost(false); notify('Role posted successfully') }}>Publish role</Button></div></Modal>}{toast && <div role="status" className="fixed bottom-5 left-1/2 z-30 -translate-x-1/2 rounded-xl bg-primary px-4 py-3 text-sm font-medium text-primary-foreground shadow-lg">{toast}</div>}</div>
}

function Overview({ nav, jobs, saved, setSaved, setSelected, apply }: any) { return <div className="flex flex-col gap-8"><section className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-sm font-medium text-primary">Tuesday, September 3</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-balance sm:text-4xl">Find work that feels like you.</h1><p className="mt-2 max-w-xl text-muted-foreground">Curated opportunities for your next chapter, based on what you care about.</p></div><Button onClick={() => nav('Find jobs')}><Search data-icon="inline-start" />Explore jobs</Button></section><div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-primary p-5 text-primary-foreground"><div className="flex items-center justify-between"><span className="text-sm opacity-75">Profile strength</span><CircleUserRound className="size-4 opacity-75" /></div><p className="mt-6 text-3xl font-semibold">86%</p><div className="mt-3 h-1.5 rounded-full bg-primary-foreground/20"><div className="h-full w-[86%] rounded-full bg-primary-foreground" /></div><button onClick={() => nav('Profile')} className="mt-4 text-sm underline underline-offset-4">Complete your profile</button></div><div className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Active applications</span><FileText className="size-4 text-muted-foreground" /></div><p className="mt-6 text-3xl font-semibold">4</p><p className="mt-3 text-sm text-muted-foreground"><span className="font-medium text-foreground">2</span> need your attention</p></div><div className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">New matches</span><Sparkles className="size-4 text-muted-foreground" /></div><p className="mt-6 text-3xl font-semibold">12</p><p className="mt-3 text-sm text-muted-foreground">Since your last visit</p></div></div><section><div className="mb-4 flex items-center justify-between"><div><h2 className="text-xl font-semibold">Recommended for you</h2><p className="mt-1 text-sm text-muted-foreground">Roles that match your profile and preferences.</p></div><button onClick={() => nav('Find jobs')} className="text-sm font-medium text-primary">View all</button></div><div className="grid gap-4 lg:grid-cols-2">{jobs.slice(0, 4).map((job: Job) => <JobCard key={job.id} job={job} saved={saved.includes(job.id)} onSave={() => setSaved((s: number[]) => s.includes(job.id) ? s.filter(x => x !== job.id) : [...s, job.id])} onOpen={() => setSelected(job)} onApply={() => setSelected(job)} />)}</div></section></div> }
function FindJobs({ query, setQuery, jobs, saved, setSaved, setSelected, apply, title = 'Find your next opportunity' }: any) { return <div className="flex flex-col gap-7"><div><p className="text-sm font-medium text-primary">Discover</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-2 text-muted-foreground">Search across thousands of thoughtful teams.</p></div><div className="flex flex-col gap-3 rounded-2xl border border-border bg-card p-3 sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl bg-muted px-3"><Search className="size-4 text-muted-foreground" /><input value={query} onChange={e => setQuery(e.target.value)} className="h-11 flex-1 bg-transparent text-sm outline-none" placeholder="Role, skill, or company" /></div><button className="flex h-11 items-center justify-center gap-2 rounded-xl border border-border px-4 text-sm font-medium hover:bg-muted"><SlidersHorizontal className="size-4" />Filters</button></div><div className="flex items-center justify-between"><p className="text-sm text-muted-foreground"><span className="font-medium text-foreground">{jobs.length} roles</span> matching your search</p><button className="flex items-center gap-1 text-sm text-muted-foreground">Sort: Relevance <ChevronDown className="size-4" /></button></div><div className="grid gap-4 lg:grid-cols-2">{jobs.map((job: Job) => <JobCard key={job.id} job={job} saved={saved.includes(job.id)} onSave={() => setSaved((s: number[]) => s.includes(job.id) ? s.filter(x => x !== job.id) : [...s, job.id])} onOpen={() => setSelected(job)} onApply={() => setSelected(job)} />)}</div>{jobs.length === 0 && <div className="rounded-2xl border border-dashed border-border py-16 text-center"><Search className="mx-auto size-8 text-muted-foreground" /><p className="mt-3 font-medium">No roles found</p><p className="mt-1 text-sm text-muted-foreground">Try a different search.</p></div>}</div> }
function Applications({ applications }: { applications: typeof seedApplications }) { return <div className="flex flex-col gap-7"><div><p className="text-sm font-medium text-primary">Your journey</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Application tracker</h1><p className="mt-2 text-muted-foreground">Keep every opportunity moving forward.</p></div><div className="grid gap-4 md:grid-cols-3">{['Applied', 'Interview', 'Offer'].map(status => <div key={status} className="rounded-2xl border border-border bg-card p-4"><div className="flex items-center justify-between"><span className="text-sm font-semibold">{status}</span><span className="rounded-full bg-muted px-2 py-1 text-xs">{applications.filter(a => a.status === status).length}</span></div><div className="mt-4 flex flex-col gap-3">{applications.filter(a => a.status === status).map(app => <div key={app.company} className="rounded-xl border border-border p-3"><div className="flex items-center gap-2"><Logo mark={app.mark} color="bg-chart-3" /><div><p className="text-sm font-semibold">{app.role}</p><p className="text-xs text-muted-foreground">{app.company}</p></div></div><p className="mt-3 text-xs text-muted-foreground">{app.date}</p></div>)}</div></div>)}</div></div> }
function Profile({ notify }: { notify: (s: string) => void }) { return <div className="max-w-3xl"><p className="text-sm font-medium text-primary">Your profile</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Show the best of you.</h1><p className="mt-2 text-muted-foreground">A complete profile helps the right opportunities find you.</p><div className="mt-7 rounded-2xl border border-border bg-card p-5 sm:p-7"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex size-20 items-center justify-center rounded-2xl bg-accent text-2xl font-semibold">JD</div><div><h2 className="text-xl font-semibold">Jordan Davis</h2><p className="mt-1 text-muted-foreground">Product designer · New York, NY</p><button className="mt-2 text-sm font-medium text-primary">Change photo</button></div></div><div className="mt-8 grid gap-5 sm:grid-cols-2"><label className="flex flex-col gap-2 text-sm font-medium">Full name<input className="h-11 rounded-xl border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring" defaultValue="Jordan Davis" /></label><label className="flex flex-col gap-2 text-sm font-medium">Headline<input className="h-11 rounded-xl border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring" defaultValue="Product designer" /></label><label className="flex flex-col gap-2 text-sm font-medium sm:col-span-2">About<textarea className="min-h-28 rounded-xl border border-input bg-background p-3 font-normal outline-none focus:ring-2 focus:ring-ring" defaultValue="I design clear, useful products for curious people." /></label></div><Button className="mt-7" onClick={() => notify('Profile saved')}>Save changes</Button></div></div> }
function Employer({ onPost, notify }: { onPost: () => void; notify: (s: string) => void }) { return <div className="flex flex-col gap-8"><section className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-medium text-primary">Hiring workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Build your next great team.</h1><p className="mt-2 text-muted-foreground">Manage roles, candidates, and hiring momentum.</p></div><Button onClick={onPost}><Plus data-icon="inline-start" />Post a role</Button></section><div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-primary p-5 text-primary-foreground"><Users className="size-5 opacity-75" /><p className="mt-6 text-3xl font-semibold">48</p><p className="mt-1 text-sm opacity-75">New applicants</p></div><div className="rounded-2xl border border-border bg-card p-5"><BriefcaseBusiness className="size-5 text-muted-foreground" /><p className="mt-6 text-3xl font-semibold">3</p><p className="mt-1 text-sm text-muted-foreground">Active roles</p></div><div className="rounded-2xl border border-border bg-card p-5"><Clock3 className="size-5 text-muted-foreground" /><p className="mt-6 text-3xl font-semibold">6.2d</p><p className="mt-1 text-sm text-muted-foreground">Avg. time to hire</p></div></div><div className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Recent applicants</h2><p className="mt-1 text-sm text-muted-foreground">Review candidates across your open roles.</p></div><button onClick={() => notify('Applicant view opened')} className="text-sm font-medium text-primary">View all</button></div><div className="mt-5 flex flex-col divide-y divide-border">{['Maya Chen', 'Alex Morgan', 'Sam Rivera'].map((name, i) => <div key={name} className="flex items-center gap-3 py-4 first:pt-0 last:pb-0"><div className="flex size-9 items-center justify-center rounded-full bg-muted text-xs font-semibold">{name.split(' ').map(n => n[0]).join('')}</div><div className="flex-1"><p className="text-sm font-medium">{name}</p><p className="text-xs text-muted-foreground">{['Product Designer', 'Design Engineer', 'Senior PM'][i]}</p></div><span className="rounded-full bg-muted px-2.5 py-1 text-xs">New</span><button className="rounded-lg p-2 text-muted-foreground hover:bg-muted"><ChevronDown className="size-4 -rotate-90" /></button></div>)}</div></div></div> }
function Modal({ children, onClose }: { children: React.ReactNode; onClose: () => void }) { return <div className="fixed inset-0 z-20 flex items-end justify-center bg-foreground/30 p-0 sm:items-center sm:p-5" role="dialog" aria-modal="true"><div className="relative max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-t-3xl bg-card p-6 shadow-xl sm:rounded-3xl"><button onClick={onClose} className="absolute right-4 top-4 rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Close dialog"><X className="size-4" /></button>{children}</div></div> }

const _unused = [Heart, Building2, Check]
